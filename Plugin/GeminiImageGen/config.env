# Gemini Image Generation Plugin Configuration (active)
# 参考: [config.env.example](config.env.example)
# 说明:
# - 可填写多个API Key, 用英文逗号分隔, 插件会随机轮询使用以提升吞吐。
# - 如需代理访问, 设置 GeminiImageProxy (示例 http://127.0.0.1:7890), 不需要则留空。
# - DIST_IMAGE_SERVERS 预留给分布式/镜像等场景(可留空), 由插件或上层策略使用。

# 多个 Key, 用英文逗号分隔 (示例: key1,key2,key3)
GeminiImageKey=YOUR_GEMINI_IMAGE_KEY_1,YOUR_GEMINI_IMAGE_KEY_2

# 代理(可选). 例如: http://127.0.0.1:7890 ; 不需要则留空
GeminiImageProxy=

# 分布式镜像服务器列表(可选), 逗号分隔URL或标识, 留空则不启用
DIST_IMAGE_SERVERS=

# 使用说明:
# 1) 本插件提供两类调用(由系统注入到提示词, 请严格按注入格式调用):
#    - GeminiGenerateImage:  文生图
#    - GeminiEditImage:      图像编辑(需提供 image_url, 支持 http/https/file://)
# 2) 不在角色卡/工具清单中硬编码字段名, 一切以系统注入的说明为“单一真源”。
# 3) 配置或变更后, 需重启 VCPToolBox 服务以生效。