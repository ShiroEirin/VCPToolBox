# NanoBananaGenOR (OpenRouter, Gemini 2.5 Flash Image Preview) - Active Config
# 参考: config.env.example
# 说明:
# - 可填写多个 OpenRouter API Key, 用英文逗号分隔; 插件会随机轮询使用以提高吞吐与稳定性。
# - 如需代理访问, 设置 OpenRouterProxy (例如 http://127.0.0.1:7890), 不需要则留空。
# - DIST_IMAGE_SERVERS 预留供分布式/本地 file:// 路径降级访问的图片镜像使用, 留空则不启用。

# 多个 OpenRouter Key (示例: sk-or-v1-xxx,sk-or-v1-yyy)
OpenRouterKeyImage=YOUR_OPENROUTER_KEY_1,YOUR_OPENROUTER_KEY_2

# 代理(可选). 例如: http://127.0.0.1:7890 ; 不需要则留空
OpenRouterProxy=

# 分布式图床服务器地址列表(可选), 用逗号分隔
# 例如: http://192.168.0.1:5974/pw=123456
DIST_IMAGE_SERVERS=

# 使用说明:
# 1) 本插件提供三类调用(由系统注入, 请严格按注入格式调用):
#    - NanoBananaGenerateImage: 文生图
#    - NanoBananaEditImage:     图像编辑(需提供 image_url, 支持 http/https/file://)
#    - NanoBananaComposeImage:  多图合成 (image_urls 数组)
# 2) 不在角色卡/工具清单中硬编码字段名, 一切以系统注入说明为“单一真源”。
# 3) 配置或变更后, 需重启 VCPToolBox 服务以生效。