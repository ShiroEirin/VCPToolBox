[2026-07-22] - 哈雷酱
⚠️ VCPChat前端模块，2026-03后新增了大量模块（Desktopmodules/Agenttaskmodules等），以下内容可能不完整
[VCP百科全书] Promptmodules - 提示词模块

【2026-09-16 积木块复制动作】（f43c3e19「feat: add prompt block copy actions」，合并提交 6443019b / PR #204；`Promptmodules/modular-prompt-module.js` +47，上游外部贡献者提交）：
- 编辑区积木块右键菜单新增『复制到小仓』，调用 `copyBlockToWarehouse(index)`：先用 `areBlocksEqual` 对当前小仓 `hiddenBlocks[this.currentWarehouse]` 查重（重复则直接返回），再 `structuredClone(block)` 深拷贝并重新 `generateId()`，推入小仓后 `save()` + `renderWarehouse()`。
- 小仓积木块右键菜单新增『复制到编辑区』，调用 `copyBlockToEditor(index)`：从 `hiddenBlocks[this.currentWarehouse]?.[index]` 取块，`structuredClone` + 新 id 后推入 `blocks`，随后 `save()` + `renderBlocks()`。
- 语义要点：复制一律生成新 id，复制体与原块不共享引用；跨仓库（编辑区 ↔ 小仓）复制不会污染来源侧；重复内容按结构相等判定被拒。
- 与既有能力的关系：小仓（hiddenBlocks）体系见本切片第三节"模块化积木块模式"，本轮只是补齐了双向复制的入口。

【2026-09-16 核心中央配置权重写（1b7d8162）】同轮另有 4 个本模块文件被改动（提交标题为"核心中央配置权重写"，具体语义未逐行核对，故只登记改动范围）：`prompt-manager.js`（124 行变化）、`modular-prompt-module.js`（65 行变化）、`preset-prompt-module.js`（63 行变化）、`original-prompt-module.js`（19 行变化）。同批还改了 `Groupmodules/grouprenderer.js`（2151 行变化）、`VCPDistributedServer/Plugin/PromptSponsor/prompt-sponsor-service.js` +21 与 `TopicSponsor/topicsponsor-service.js` +21，以及两个插件 manifest（各 6 行）。

一、模块基本信息
1. 模块名称：Promptmodules / 提示词模块
2. 模块类型：前端UI模块
3. 版本号：1.0.0
4. 所属项目：VCPChat
5. 功能一句话总结：提供三种系统提示词编辑模式（原始富文本、模块化积木块、临时与预制）

二、功能定位
Promptmodules是VCPChat的系统提示词管理模块，提供三种不同的提示词编辑模式。满足从简单直接的文本编辑到复杂的模块化组合的各种使用场景。

三、核心功能
1. 原始富文本模式：
   - 简单直观的文本域输入
   - 自动高度调整
   - 向后兼容旧字段
   - 存储字段：originalSystemPrompt

2. 模块化积木块模式：
   - 拖拽排序积木块
   - 文本积木块（可编辑、多内容条目）
   - 换行积木块
   - 小仓库系统（全局仓库、私有仓库）
   - 多内容条目（Variants）功能
   - 存储字段：advancedSystemPrompt

3. 临时与预制模式：
   - 预设文件夹管理
   - 占位符替换
   - 快速切换场景
   - 存储字段：presetSystemPrompt

4. 管理功能：
   - 三种模式切换
   - 模式名称自定义
   - 右键菜单操作
   - 数据持久化

四、主要文件结构
- prompt-manager.js：主管理器（13KB）
- original-prompt-module.js：原始富文本模块
- modular-prompt-module.js：模块化积木块模块（56KB）
- preset-prompt-module.js：临时与预制模块（10KB）
- prompt-modules.css：统一样式

五、关键API接口
1. PromptManager类：
   - init(options)：初始化管理器
   - switchMode(mode)：切换模式
   - save()：保存数据
   - getPrompt()：获取提示词

2. OriginalPromptModule类：
   - render(container)：渲染UI
   - save()：保存数据
   - getPrompt()：获取提示词

3. ModularPromptModule类：
   - render(container)：渲染UI
   - addBlock(type)：添加积木块
   - removeBlock(blockId)：删除积木块
   - save()：保存数据

4. PresetPromptModule类：
   - render(container)：渲染UI
   - loadPreset(presetId)：加载预设
   - save()：保存数据

六、使用场景
1. 简单提示词：使用原始富文本模式
2. 复杂提示词：使用模块化积木块模式
3. 预设场景：使用临时与预制模式

七、与其他模块的关系
- 依赖 Electron API：与主进程通信
- 集成 VCPChat主题系统

八、注意事项
1. 三种模式独立存储
2. 支持模式切换
3. 积木块支持拖拽排序
4. 小仓库系统支持全局和私有

Tag: 提示词管理, 三种模式, 模块化积木块, 预设管理, 占位符替换, TagMemo V9.1
