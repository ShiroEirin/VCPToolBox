[2026-07-22] - 哈雷酱
[VCP百科全书] 插件说明——VCPTaskAssistant（原子切片）

【2026-09-16 持久化原子性与熔断保护】（cf60e620，合并提交 cdc3e5e1，来源 PR #466 infinite-vector/fix/task-assistant-atomic-persistence；vcp-task-assistant.js +96/-14，现 936 行）：
- 新增镜像备份文件 `task-center-data.json.bak`（BAK_FILE，:177）。
- `saveDisabled` 熔断安全锁（:178）：主文件与备份都无法有效解析时不再降级为空壳数据，直接抛错并锁死写盘（:232-233）；锁生效期间 `saveData()` 立即返回并打印告警（:239）。
- `tryParse(filePath)`（:182）：文件不存在或内容为空返回 null；**体积异常拦截**——文件大于 1KB 但解析后 tasks 数量为 0 时抛「文件体积异常(...B)但无有效任务，触发安全拦截」。
- 读取顺序改为：主文件解析失败 → 打印损坏日志 → 尝试 `.bak` 恢复；主备皆不存在才创建默认数据并落盘，主备皆存在却都无法解析则抛错保护。
- 写入侧改为 FIFO 串行队列 `saveQueue = saveQueue.then(...)`（:245），同一时刻仅一个写事务，消除并发写撕裂；提交标题自述手段为 atomic rename + save queue。
- 临时文件 `task-center-data.json.<时间戳>_<随机串>.tmp`（:247），随机后缀避免多次写入互相踩踏。
- **空壳反向覆写拦截**（:253-259）：磁盘原文件大于 10KB 而当前待写内容小于 1KB 且无任务时判定为恶性覆写，拒绝写入并置 `saveDisabled = true`。
- 其他加固：读取恢复旧脏状态时改为 `if (task.runtime && task.runtime.running)`，容忍 runtime 字段缺失。

一、插件身份
- name: `VCPTaskAssistant`
- displayName: `VCP任务派发中心`
- version: `2.1.0`
- pluginType: `hybridservice`
- author: `Roo`
- entryPoint: `vcp-task-assistant.js`（script模式）
- protocol: `direct`
- command: 无（通过管理面板配置和触发）

二、核心能力
- 统一管理定时任务、一次性任务、通用指令派发与论坛巡航任务。
- 支持可编辑提示词模板、多 Agent 目标列表、临时工具注入及论坛帖子列表预读取注入。
- 通过管理面板进行可视化配置与手动触发。
- invocationCommands: 无（通过 capabilities 为空，由管理面板控制）

三、工作机制
PluginManager 加载 → `initialize(config, dependencies)` → 读取任务配置模板 → 注册 cron 定时器 → 到达触发时间时构造提示词 → 唤醒目标 Agent → 注入临时工具和论坛帖子上下文。

四、使用/排障边界
- 适合需要按时间表自动唤醒 Agent 处理任务的场景。
- 提示词模板和 Agent 目标列表通过管理面板配置，不支持直接编辑配置文件。
- 论坛帖子预读取依赖 VCPForumOnline 配置。
- 运行时数据存储：task-center-data.json

五、历史与兼容性
- 当前版本 2.1.0，相比早期版本增加了临时工具注入和论坛帖子预读取功能。
- 任务数据持久化到 task-center-data.json，支持热重载。

六、待验证项
- cron 注册和定时触发端到端流程。
- 临时工具注入的实际行为。

lib目录包含任务调度的核心逻辑模块
支持一次性任务和循环定时任务两种模式
临时工具注入允许Agent在任务执行时使用额外插件能力

Tag: VCPTaskAssistant, vcp-task-assistant.js, hybridservice, cron, 定时任务, Agent派发, 论坛巡航, 提示词模板, task-center-data.json, TagMemo V9.1
